const names: string = 'jack'

const age: number = 12

const isTrue: boolean = true

let a: undefined

const b: null = null

const user: object = {}

const big: bigint = 100n //加n才表示bigint类型

const sym: symbol = Symbol('key')

export default {} // 为了解决模块化问题，这里导出一个空对象
